{
  "name": "@local/openclaw-tinyfish",
  "version": "1.0.0",
  "type": "module",
  "description": "OpenClaw plugin wrapping tinyfish-cli for browser automation",
  "license": "UNLICENSED",
  "peerDependencies": {
    "openclaw": ">=2026.3.13"
  },
  "scripts": {
    "check": "node --check index.js && node --check lib/config.js && node --check lib/runner.js && node --check lib/tools.js"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  }
}
